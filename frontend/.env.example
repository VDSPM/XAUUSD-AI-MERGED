# Base URL for the future backend API.
# All services in src/services/ currently ignore this and resolve mock data;
# once the backend exists, point this at it and swap the mock resolvers for
# real fetch calls.
VITE_API_BASE_URL=/api
